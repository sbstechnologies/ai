# Streamlit Cloud Production Secrets Configuration Example
# Copy or paste the following into Streamlit Cloud -> App Settings -> Secrets

db_type = "mysql"  # Set to "mysql" for production MySQL database, or "sqlite" for built-in local DB

[mysql]
host = "localhost"
port = 3306
user = "root"
password = ""
database = "face_ai"
